import React from 'react';

const Home = () => (
  <div>
    <div>Honey I am the very BEST home component!</div>
    <button onClick={() => console.log('Pressed Me!')}>Press Me!</button>
  </div>
);
export default Home;
